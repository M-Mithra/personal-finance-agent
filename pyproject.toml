[project]
name = "personal-finance-agent"
version = "0.1.0"
description = "Personal transaction and spending analysis with deterministic, verifiable analytics"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
